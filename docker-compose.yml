version: '2'
services:
  neo4j:
    image: neo4j
    environment:
      - NEO4J_AUTH=none
  api:
    build: ./api
    links:
      - neo4j
    depends_on:
      - neo4j
  nginx:
    build: ./nginx
    ports:
      - "80:80"
    links:
      - api
    depends_on:
      - api
